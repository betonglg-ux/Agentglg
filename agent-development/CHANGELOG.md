# CHANGELOG GitHub-зеркала

Репозиторий зеркала: `betonglg-ux/Agentglg`

Этот файл фиксирует значимые изменения в развитии агента, его экспортного пакета и материалов для будущего клонирования.

## 2026-05-21

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией зеркала: локальная память в `/workspace/memory/memory/` подтверждена как первичный источник истины;
- отдельно сверены рабочая память, подтверждённые пользовательские правки и доступные защитные дубли в `agent-development/memory-exports/raw-memory/`; отдельного каталога `memory/snapshots/` в текущей среде не обнаружено;
- подтверждено, что `AGENTS.md`, `/workspace/memory/AGENTS.md` и основная зеркальная копия инструкций уже соответствуют актуальной рабочей среде и не требуют перезаписи;
- выявлено реальное отставание только у вложенного дубля `agent_files/agent-development/` внутри зеркала: там оставались лишние служебные файлы и устаревшие версии `CHANGELOG.md` и `files-index/templates-index.md`, которых уже нет в живой папке `/workspace/agent_files/agent-development/`;
- вложенная копия `agent_files/` фактически выровнена строго по текущей рабочей среде без обратной подмены локальной памяти данными зеркала.

### Примечания
- несущностное отличие между `memory/confirmed-error-patterns.md` и `agent-development/confirmed-error-patterns.md` по-прежнему ограничивается завершающим переводом строки и не потребовало содержательной перезаписи;
- синхронизация выполняется только в сторону `текущий агент -> GitHub-зеркало`.

## 2026-05-21

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией зеркала: локальная память в `/workspace/memory/memory/` подтверждена как первичный источник истины;
- отдельно сверены рабочая память, подтверждённые пользовательские правки и свежий защитный снимок `memory/snapshots/20260521T125814Z`; содержательных расхождений, требующих восстановления из зеркала, не обнаружено;
- подтверждено, что `AGENTS.md` и основная копия инструкций в зеркале уже соответствуют актуальной рабочей среде и не требуют перезаписи;
- выявлено реальное отставание только у вложенного дубля `agent_files/agent-development/` внутри зеркала: там оставались лишние служебные файлы и устаревшие копии, которых уже нет в живой папке `/workspace/agent_files/agent-development/`;
- вложенная копия `agent_files/` выровнена строго по текущей рабочей среде без обратной подмены локальной памяти данными зеркала.

### Примечания
- несущностное отличие между `memory/confirmed-error-patterns.md` и `agent-development/confirmed-error-patterns.md` по-прежнему ограничивается завершающим переводом строки и не потребовало содержательной перезаписи;
- синхронизация выполняется только в сторону `текущий агент -> GitHub-зеркало`.

## 2026-05-20

### Дополнено
- при повторной сверке перед выгрузкой подтверждено, что удалённый `origin/master` и локальный `HEAD` совпадали до правок;
- выявлено и фактически устранено реальное отставание только у вложенного зеркального дубля `agent_files/agent-development/`: он содержал лишние служебные файлы и устаревшие версии `CHANGELOG.md` и `files-index/templates-index.md`, которых уже нет в живой папке `/workspace/agent_files/agent-development/`;
- вложенная копия `agent_files/` повторно выровнена строго по текущей рабочей среде без обратной подмены локальной памяти или инструкций данными GitHub-зеркала.

### Примечания
- рабочая память в `/workspace/memory/memory/` не переписывалась данными зеркала;
- содержательных расхождений между рабочей памятью, подтверждёнными пользовательскими правками и основными зеркальными дублями не выявлено.

## 2026-05-20

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией зеркала: локальная память в `/workspace/memory/memory/` подтверждена как первичный источник истины;
- отдельно сверены рабочие memory-файлы, подтверждённые пользовательские правки и защитные дубли; каталога `memory/snapshots/` с файлами в текущей среде не обнаружено, а содержательных расхождений между рабочей памятью и её основными зеркальными копиями не найдено;
- подтверждено, что `AGENTS.md`, `/workspace/memory/AGENTS.md` и `agent-development/current-agent-instructions.md` уже соответствуют актуальной рабочей среде и не требуют обратной подмены;
- выявлено реальное отставание только у вложенной копии `agent_files/` внутри зеркала: в `agent_files/agent-development/` оставались лишние служебные файлы и устаревшие версии `CHANGELOG.md` и `files-index/templates-index.md`, которых нет в живой папке `/workspace/agent_files/agent-development/`;
- вложенная копия `agent_files/` выровнена строго по текущей рабочей среде без переписывания локальной памяти данными GitHub-зеркала.

### Примечания
- удалённый `origin/master` после `fetch` совпадает с локальным `HEAD` до новых изменений;
- синхронизация выполняется только в сторону `текущий агент -> GitHub-зеркало`.

## 2026-05-17

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией зеркала: локальная память в `/workspace/memory/memory/` подтверждена как первичный источник истины;
- отдельно сверены рабочие memory-файлы, подтверждённые пользовательские правки и доступные защитные снимки; каталога `memory/snapshots/` с файлами в текущей среде не обнаружено, а содержательных расхождений между рабочей памятью и её основными зеркальными дублями не найдено;
- подтверждено, что `AGENTS.md` и `agent-development/current-agent-instructions.md` уже соответствуют актуальной рабочей среде и не требуют обратной подмены;
- выявлено реальное отставание только у вложенной зеркальной копии `agent_files/agent-development/`: в ней оставались лишние служебные файлы и устаревшие версии `CHANGELOG.md` и `files-index/templates-index.md`, которых нет в живой папке `/workspace/agent_files/agent-development/`;
- вложенная копия `agent_files/` выровнена строго по текущей рабочей среде без переписывания локальной памяти данными GitHub-зеркала.

### Примечания
- синхронизация выполняется только в сторону `текущий агент -> GitHub-зеркало`;
- несущностное отличие между `memory/confirmed-error-patterns.md` и `agent-development/confirmed-error-patterns.md` ограничивалось завершающим переводом строки и не потребовало содержательной перезаписи.

## 2026-05-16

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией зеркала: локальная память в `/workspace/memory/memory/` подтверждена как первичный источник истины, а удалённые `HEAD` и `origin/master` не расходятся с локальной веткой;
- отдельно сверены рабочие memory-файлы, подтверждённые пользовательские правки и доступные защитные снимки; каталога `memory/snapshots/` с файлами в текущей среде не обнаружено, содержательных расхождений между рабочей памятью и основными зеркальными дублями не найдено;
- подтверждено, что `AGENTS.md` и `agent-development/current-agent-instructions.md` уже соответствуют актуальной рабочей среде и не требуют обратной подмены;
- выявлено реальное содержательное отставание только у вложенного служебного дубля `agent_files/agent-development/` внутри зеркала: там оставались лишние служебные файлы и устаревшие версии `CHANGELOG.md` и `files-index/templates-index.md`, которых нет в живой папке `/workspace/agent_files/agent-development/`;
- вложенный дубль `agent_files/agent-development/` выровнен строго по текущей рабочей среде checksum-сверкой без переписывания локальной памяти данными GitHub-зеркала.

### Примечания
- синхронизация выполнена только в сторону `текущий агент -> GitHub-зеркало`;
- несущностное отличие между `memory/confirmed-error-patterns.md` и `agent-development/confirmed-error-patterns.md` ограничивалось завершающим переводом строки и не потребовало содержательной перезаписи.

## 2026-05-16

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией зеркала: локальная память в `/workspace/memory/memory/` подтверждена как первичный источник истины;
- отдельно сверены рабочие memory-файлы, подтверждённые пользовательские правки и доступные защитные слои; каталога `memory/snapshots/` с файлами в текущей среде не обнаружено, содержательных расхождений между памятью и зеркальными дублями не подтверждено;
- подтверждено, что `AGENTS.md` и основная зеркальная копия `agent-development/current-agent-instructions.md` уже соответствуют актуальной рабочей среде и не требуют обратной подмены;
- выявлено реальное отставание только у вложенного служебного дубля `agent_files/agent-development/`: он содержал лишние служебные файлы и устаревшие версии `CHANGELOG.md` и `files-index/templates-index.md`, которых нет в живой папке `/workspace/agent_files/agent-development/`;
- вложенный дубль `agent_files/agent-development/` выровнен строго по текущей рабочей среде без переписывания локальной памяти данными GitHub-зеркала.

### Примечания
- синхронизация выполнена только в сторону `текущий агент -> GitHub-зеркало`;
- несущностное отличие между `memory/confirmed-error-patterns.md` и `agent-development/confirmed-error-patterns.md` ограничивалось завершающим переводом строки и не потребовало содержательной перезаписи.

## 2026-05-16

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией зеркала: локальная память в `/workspace/memory/memory/` подтверждена как первичный источник истины;
- отдельно сверены рабочие memory-файлы, подтверждённые пользовательские правки и доступные защитные снимки; содержательных расхождений, требующих восстановления из зеркала, не обнаружено, а каталога `memory/snapshots/` с файлами в текущей среде нет;
- подтверждено, что живые источники `/workspace/AGENTS.md` и `/workspace/agent_files/` уже совпадают с локальным зеркальным checkout и не требуют дополнительной выгрузки;
- выявлено реальное несоответствие только в локальной автоматизации синхронизации: она всё ещё местами считала веткой зеркала `main`, хотя удалённый `HEAD` репозитория `betonglg-ux/Agentglg` указывает на `master`;
- локальные файлы автоматизации и их переносимый raw-memory-дубль выровнены под фактическую ветку `master`, после чего изменение подготовлено к синхронизации в GitHub-зеркало.

### Дополнено
- при повторной сверке перед выгрузкой подтверждено, что `origin/master` и локальная ветка не расходятся;
- содержательное отличие найдено только во вложенном зеркальном дубле `agent_files/agent-development/`: в нём оставались устаревшие служебные файлы, которых уже нет в живой папке `/workspace/agent_files/agent-development/`;
- вложенный дубль `agent_files/` повторно выровнен строго по текущей рабочей среде через checksum-сверку без обратной подмены локальной памяти или инструкций данными GitHub-зеркала.

### Примечания
- рабочая память не переписывалась данными зеркала;
- синхронизация выполняется только в сторону `текущий агент -> GitHub-зеркало`.

## 2026-05-15

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией зеркала: локальная память в `/workspace/memory/memory/` подтверждена как первичный источник истины;
- отдельно сверены рабочие memory-файлы, подтверждённые пользовательские правки и доступные защитные снимки; каталога `memory/snapshots/` с файлами в текущей среде не обнаружено;
- подтверждено, что `AGENTS.md`, основные зеркальные копии памяти и переносимые инструкции уже соответствуют локальной среде и не требуют обратной подмены данными GitHub-зеркала;
- выявлено реальное отставание только у вложенного дубля `agent_files/agent-development/` внутри зеркала: он содержал лишние служебные файлы и устаревшие версии `CHANGELOG.md` и `files-index/templates-index.md`;
- вложенный дубль `agent_files/agent-development/` выровнен строго по текущей живой папке `/workspace/agent_files/agent-development/`.

### Примечания
- рабочая память не переписывалась данными зеркала;
- синхронизация выполняется только в сторону `текущий агент -> GitHub-зеркало`.

## 2026-05-14

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией: локальная память в `/workspace/memory/memory/` подтверждена как первичный источник истины;
- отдельно сверены рабочие memory-файлы, каталог защитных снимков `memory/snapshots/` и подтверждённые пользовательские правки; защитных снимков в текущей среде не найдено, а содержательных расхождений между рабочей памятью и зеркальными дублями не подтверждено;
- подтверждено, что `AGENTS.md` и переносимая копия инструкций уже совпадают с актуальной рабочей средой и не требуют перезаписи;
- выявлено реальное отставание только у вложенного служебного дубля `agent_files/agent-development/`: он содержал лишние экспортные файлы и устаревшие служебные копии, которых уже нет в живой папке `/workspace/agent_files/`;
- вложенный служебный дубль `agent_files/agent-development/` выровнен строго по текущей рабочей среде без обратной подмены памяти или инструкций данными GitHub-зеркала.

### Примечания
- рабочая память не переписывалась данными зеркала;
- синхронизация выполняется только в сторону `текущий агент -> GitHub-зеркало`.

## 2026-05-13

### Изменено
- при повторной проверке текущей рабочей среды подтверждено, что содержательных расхождений между `/workspace/memory/AGENTS.md`, `agent_files/`, рабочей памятью и зеркальными дублями нет; `confirmed-error-patterns.md` отличался только завершающим переводом строки;
- локальная синхронизация исправлена для текущей среды: если запуск идет из checkout самого зеркала, `memory/automation/sync_agentglg_mirror.py` теперь использует уже настроенный `origin`, включая backend-remote `chatgpt.com/backend-api/git-authed/...`, вместо принудительного возврата к прямому `github.com`;
- обновлены описание автоматизации в `memory/automation/README.md` и соответствующий raw-memory-дубль, чтобы правило использования текущего backend-remote было зафиксировано в переносимых материалах;
- после правки сухой прогон синхронизации снова проходит в текущей среде и подтверждает отсутствие новых содержательных изменений для выгрузки.

### Примечания
- локальная память в `/workspace/memory/memory/` осталась первичным источником истины и не переписывалась данными зеркала;
- при проверке доступных защитных снимков каталог `memory/snapshots/` в текущей среде не содержал файлов, поэтому сверка выполнялась по рабочей памяти и подтверждённым пользовательским правкам.

## 2026-05-13

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией зеркала и подтверждено, что локальная память в `/workspace/memory/memory/` остаётся первичным источником истины;
- перед обновлением зеркала отдельно сверены рабочие memory-файлы, доступный защитный снимок `memory/snapshots/20260513T144154Z` и подтверждённые пользовательские правки;
- подтверждено, что `AGENTS.md`, рабочая память и основные экспортные дубли не требуют содержательной перезаписи из зеркала;
- выявлены реальные расхождения только во вложенной зеркальной копии `agent_files/agent-development/`: она отставала от текущего `/workspace/agent_files/` и содержала лишние служебные дубли;
- вложенная копия `agent_files/agent-development/` выровнена по текущей рабочей среде: обновлены `CHANGELOG.md` и `files-index/templates-index.md`, удалены отсутствующие в живой папке дополнительные служебные файлы.

### Примечания
- рабочая память не переписывалась данными из GitHub-зеркала;
- синхронизация выполняется только в сторону `текущий агент -> GitHub-зеркало`.

## 2026-05-13

### Изменено
- повторно проверена текущая рабочая среда агента перед синхронизацией зеркала: локальная память в `/workspace/memory/memory/` подтверждена как первичный источник истины;
- отдельно сверены рабочие memory-файлы, подтверждённые пользовательские правки и доступные защитные слои; перед записью создан локальный защитный снимок в рабочем снимке синхронизации `memory/snapshots/20260513T141427Z`;
- подтверждено, что `AGENTS.md`, переносимые инструкции, `agent_files/`, рабочая память и основные экспортные дубли не требуют содержательной перезаписи;
- выполнен автоматический экспорт в GitHub-зеркало и пересборка служебных индексов.

## 2026-05-05

### Изменено
- обновлены экспортные файлы памяти в `agent-development/memory-exports/` подтверждёнными пользовательскими правками и правилами проверки;
- в `user-corrections-export.md` перенесён свод подтверждённых правил, частных кейсов и правил работы с памятью и GitHub-зеркалом;
- в `template-notes-export.md` перенесены устойчивые заметки по шаблонам УК, УИ, КУ, ВС и общим правилам оформления;
- в `missed-findings-export.md` зафиксированы подтверждённые случаи пропущенных замечаний и контрольные точки, которые нельзя пропускать в будущем;
- в `confirmed-error-patterns-export.md` зафиксированы подтверждённые паттерны ошибок, исключения и правила повышенного внимания;
- синхронизирован `agent-development/current-agent-instructions.md` с текущими реальными инструкциями агента;
- обновлены сводные файлы полного зеркала: `agent-development/agent-summary.md`, `agent-development/github-export-bundle.md`, `agent-development/memory-exports/README.md` и `agent-development/github-mirror-manifest.md`;
- обновлены файловые индексы `agent-development/files-index/attached-files-index.md` и `agent-development/files-index/templates-index.md` по текущему видимому дереву файлов агента;
- создана симметричная структура между рабочими файлами памяти агента и зеркальными файлами в `agent-development/`: добавлены `agent-development/confirmed-error-patterns.md`, `agent-development/missed-findings-log.md`, `agent-development/template-notes.md`, `agent-development/user-confirmed-corrections.md`, `agent-development/user-preferences.md`, `agent-development/slack-user-corrections.md` и `agent-development/memory-save-log.md`;
- инструкции агента дополнительно уточнены так, чтобы симметрия `Memory <-> agent-development/` была закреплена как обязательный стандарт сопровождения;
- манифест зеркала обновлён под симметричную структуру памяти и теперь явно фиксирует одноимённые пары рабочих memory-файлов и зеркальных файлов в `agent-development/`;
- экспортные файлы `agent-development/memory-exports/user-corrections-export.md` и `agent-development/memory-exports/missed-findings-export.md` перезаписаны содержимым актуальных рабочих memory-файлов;
- добавлены новые экспортные файлы `agent-development/memory-exports/user-preferences-export.md` и `agent-development/memory-exports/memory-save-log-export.md`;
- в инструкции агента добавлено обязательное правило вести `memory-save-log.md` как журнал успешных и неуспешных событий сохранения памяти;
- `agent-development/memory-exports/README.md` и `agent-development/github-export-bundle.md` обновлены под новые export-файлы и актуальную схему памяти;
- в сводных файлах, индексах и структуре памяти закреплён рутинный режим: зеркальные markdown-файлы должны обновляться как обычная обязательная часть сопровождения агента после значимых изменений;
- в основные инструкции агента добавлено жёсткое исключение для протоколов КП: отсутствие нижнего колонтитула не считать ошибкой и нумерацию страниц не проверять;
- файловая копия инструкций в `agent-development/current-agent-instructions.md` обновлена под это же правило для последующей синхронизации в GitHub.

### Примечания
- локальная память остаётся первичным источником истины, а экспортные markdown-файлы служат файловым дублем для зеркала;
- содержимое подготовлено для синхронизации в GitHub-зеркало, но само внешнее обновление зеркала должно выполняться отдельно доступным каналом синхронизации.

## 2026-04-29

### Добавлено
- создан манифест GitHub-зеркала: `agent-development/github-mirror-manifest.md`;
- создан индекс текущих файлов агента: `agent-development/files-index/attached-files-index.md`;
- создан индекс шаблонов протоколов: `agent-development/files-index/templates-index.md`;
- сохранена переносимая копия текущих инструкций агента: `agent-development/current-agent-instructions.md`;
- создана краткая карточка агента: `agent-development/agent-summary.md`;
- создан индекс обязательного навыка: `agent-development/skills/README.md`;
- собран стартовый экспортный пакет: `agent-development/github-export-bundle.md`;
- подготовлена папка экспорта Memory: `agent-development/memory-exports/`;
- созданы заготовки файлов для экспорта подтверждённых наработок из Memory;
- создан пошаговый план восстановления агента из зеркала: `agent-development/recovery-plan.md`;
- создан файл changelog: `agent-development/CHANGELOG.md`.

### Изменено
- правила агента обновлены так, чтобы текущая конфигурация агента оставалась основной рабочей средой;
- GitHub закреплён как внешний дублирующий архив для файлов, навыков, экспортов Memory и материалов будущего клонирования;
- в инструкциях закреплён режим, где GitHub не заменяет агент, а хранит его переносимое зеркало.

### Примечания
- текущий экспортный комплект является стартовой базой и будет дополняться по мере инвентаризации файлов, навыков и данных Memory;
- список файлов агента пока частичный, потому что видимое файловое дерево в редакторе отображается не полностью.