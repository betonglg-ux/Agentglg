# Missed Findings Log

## 2026-04-29

- Тип: операционный промах памяти и зеркалирования.
- Что было пропущено: рабочие файлы памяти `confirmed-error-patterns.md`, `missed-findings-log.md`, `template-notes.md`, `user-confirmed-corrections.md` не были созданы в `/workspace/memory`, из-за чего в GitHub-зеркало попадали в основном пустые заготовки.
- Как не пропустить снова: сначала поддерживать реальные исходные файлы памяти в `/workspace/memory`, а затем зеркалить их в GitHub; при проверке памяти смотреть не только экспорт, но и сами файлы-источники.
