# Журнал пропущенных замечаний

## 2026-04-28

- Тип протокола: УК, схема Г, приложение Б.
- Что было пропущено: несоответствие конструкции в таблице 2 приложения Б данным основного отчета.
- Как не пропустить снова: отдельно сверять таблицу 2 приложения Б с основным отчетом по конструкциям, а не ограничиваться общей визуальной проверкой.

## 2026-04-28

- Тип протокола: УК с приложениями-схемами.
- Что было пропущено: некорректное окончание `Схема` / `Схемы` в единственном приложении и привязка приложения к конкретному отчету.
- Как не пропустить снова: определять принадлежность приложения по номеру в нижнем колонтитуле и проверять формулировку именно в самом приложении.

## 2026-04-28

- Тип протокола: разные типы, строки с наименованием конструкций.
- Что было пропущено: орфографическая ошибка в названии конструкции.
- Как не пропустить снова: проверять не только числа и колонтитулы, но и текстовые наименования конструкций на явные опечатки.

## 2026-04-29

- Тип: операционный промах памяти и зеркалирования.
- Что было пропущено: рабочие файлы памяти `confirmed-error-patterns.md`, `missed-findings-log.md`, `template-notes.md`, `user-confirmed-corrections.md` не были заведены как реальные содержательные файлы, из-за чего в зеркало уходили технические заготовки.
- Как не пропустить снова: поддерживать память сначала в исходных файлах `/workspace/memory/*.md`, а затем синхронизировать ее в GitHub.

## 2026-04-30

- Тип: операционный промах сохранения памяти.
- Что было пропущено: подтвержденные пользователем правила для КУ-протоколов от `2026-04-30` не попали в постоянный файл `user-confirmed-corrections.md`, поэтому исчезли из зеркала при следующей синхронизации.
- Как не пропустить снова: после каждого явного подтверждения пользователя сразу записывать правило в соответствующий файл памяти и затем синхронизировать зеркало.

## 2026-04-30

- Тип: операционный промах сохранения памяти из Slack-диалога.
- Что было пропущено: подтвержденное пользователем правило по формату абсолютных и относительных отметок осталось только в сообщении диалога и не было перенесено в постоянные файлы памяти.
- Как не пропустить снова: каждое сообщение вида `сохранил правило в память` сразу подтверждать реальной записью в `user-confirmed-corrections.md` и связанном профильном файле памяти.

## 2026-04-30

- Тип: операционный промах при работе с памятью и GitHub-зеркалом.
- Что было пропущено: агент допустил опасную логику, при которой удаленное состояние могло восприниматься как более актуальное, чем локальная память, хотя для этого агента источник истины должен быть обратным.
- Как не пропустить снова: считать локальную память в `/workspace/memory` первичной, использовать GitHub только как одностороннее зеркало и не обновлять память агента из зеркала без явной команды пользователя и ручной сверки.

## 2026-05-04

- Тип протокола: УК, `Приложение А`, легенда схемы.
- Что было пропущено: на схеме указана дата бетонирования, которой нет в основном протоколе.
- Как не пропустить снова: отдельно сверять каждую дату в легенде схемы с таблицей дат бетонирования основного протокола, а не ограничиваться общей сверкой номера приложения.

## 2026-05-04

- Тип протокола: УК, визуальный дефект таблицы.
- Что было пропущено: локальный разрыв нижней границы таблицы в правой части блока значений рядом с датой и числом.
- Как не пропустить снова: просматривать нижние и правые участки рамок локально по каждому блоку значений, даже если вся таблица в целом выглядит замкнутой.

## 2026-05-04

- Тип: операционный промах коммуникации о памяти.
- Что было пропущено: агент писал пользователю, что уже сохранил правило в память, хотя реальная запись в постоянные файлы еще не была сделана или не была проверена.
- Как не пропустить снова: сначала коротко сообщать о действии сохранения, затем реально записывать правило в память, затем отдельно подтверждать результат только после проверки, что запись действительно появилась в нужном файле.
