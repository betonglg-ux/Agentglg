# Подтвержденные пользователем поправки

- 2026-04-28. Пользователь явно подтвердил правило: для их протоколов допустимо, когда у конструкции возраст `28` суток, а в выводе написано `более 28 суток`. Такой случай не считать замечанием и не упоминать в будущих проверках.
- 2026-04-28. Пользователь явно подтвердил дополнительное правило для УК-протоколов по схеме Г с приложением Б: нужно проверять, что значения `Hi` из таблицы 1 приложения Б указаны в отчете(ах), а данные таблицы 2 приложения Б идентичны данным отчета. Для файла `ЛВ-Строй Заморенова от 24.04.2026.pdf` подтверждена реальная ошибка: в таблице 2 приложения Б указана неверная конструкция, которой нет в основном отчете.
- 2026-04-28. Пользователь явно подтвердил правило: для протоколов типа `№.КП` нижний колонтитул не предусмотрен. Отсутствие нижнего колонтитула в КП-протоколах не считать ошибкой и не выносить в замечания.
- 2026-04-28. Пользователь явно подтвердил правило: номер отчета или протокола должен иметь вид `номер.подномер.идентификатор`, например `253.1.УК`. Подномер начинается с `1` и дальше идет по количеству протоколов в серии. Буквенный идентификатор указывает тип: `КП`, `УК`, `КУ` и т.д.
- 2026-04-28. Пользователь уточнил: если в отчетах одинаковое уравнение градуировочной зависимости, но разный коэффициент `b`, это не считать автоматической ошибкой, но обязательно выносить как замечание уровня внимания или на уточнение.
- 2026-04-28. Пользователь явно подтвердил дополнительное правило: нужно проверять орфографические ошибки в наименованиях конструкций. Для файла `1.pdf` подтверждена реальная ошибка в строке конструкции: `Плита пеекрытия в/о 8/1-12//А-Г на отм. +9,800` должно быть `Плита перекрытия в/о 8/1-12//А-Г на отм. +9,800`.
- 2026-04-28. Пользователь явно подтвердил дополнительное правило для УК-протоколов с приложениями-схемами: принадлежность `Приложения А` к конкретному отчету нужно отслеживать по номеру в нижнем колонтитуле. Если у протокола только одно приложение со схемой, то некорректное окончание `Схема` / `Схемы` нужно фиксировать именно в приложении. Для файла `Капитель, Шепелюгиснкая з.у 7 от 23.04.2026 (есть недоборы).pdf` подтвержденные локализации: стр. `15` для `94.3.УК` и стр. `28` для `94.6.УК`.
- 2026-04-29. Пользователь явно подтвердил правило для УК/УИ-протоколов: если дата бетонирования указана диапазоном, возраст указывается по крайней дате бетонирования. Для файла `КР строительство 2-я Магистральная от 23.04.2026.pdf` строка `15.03.2026 - 16.03.2026` с возрастом `38 суток` корректна, потому что крайняя дата `16.03.2026` соответствует `38` суткам.
- 2026-04-30. Пользователь явно подтвердил правило для КУ-протоколов: нужно всегда проверять соответствие вывода наименованию участка в таблице. Если в таблице указан один участок, форма `обследуемых участков` в выводе является ошибкой; множественное число допустимо только при нескольких участках. В выводе обязательно должен быть указан тип участка из таблицы. Для обратной засыпки корректная логика формулировки: `...на обследованном участке обратной засыпки...`.
- 2026-04-30. Пользователь явно уточнил правило для КУ-протоколов: нельзя придумывать названия вывода от себя. Нужно использовать и проверять именно стиль шаблонной фразы, где меняется только число и тип основания. Подтвержденные пары: `одно основание / песчаное основание -> Коэффициент уплотнения обследуемого участка песчаного основания, на момент...`; `несколько оснований / песчаное основание -> Коэффициент уплотнения обследуемых участков песчаного основания, на момент...`; `одно основание / обратная засыпка -> Коэффициент уплотнения обследуемого участка обратной засыпки, на момент...`; `несколько оснований / обратная засыпка -> Коэффициент уплотнения обследуемых участков обратной засыпки, на момент...`; `одно основание / грунтовое основание -> Коэффициент уплотнения обследуемого участка грунтового основания, на момент...`; `несколько оснований / грунтовое основание -> Коэффициент уплотнения обследуемых участков грунтового основания, на момент...`.
- 2026-04-30. Пользователь явно подтвердил правило по отметкам: абсолютные отметки писать без знака, в метрах, с точностью до тысячных, например `140,000`. Относительные отметки писать со знаком `+` или `-`, кроме `0,000`. Разделитель в отметках использовать только запятую: запись `140,200` корректна, запись `140.200` некорректна.
- 2026-04-30. Пользователь явно подтвердил правило для ВС-протоколов: отсутствие поля `Материал по проекту` не считать ошибкой. Если материал известен и его логично указать, добавлять только напоминание в конце отчета и не выносить этот случай в список замечаний по страницам.
- 2026-04-30. Пользователь явно подтвердил правило для шапки ВС-протоколов: не проверять упоминание ГОСТ в шапке и не выносить по этой строке замечание.
- 2026-04-30. Пользователь явно подтвердил частное правило: строку удостоверения из файла `ПСО-13 Мытищи, д.Челобитьево от 30.04.2026.pdf` не считать ошибкой в будущих проверках.
- 2026-04-30. Пользователь явно подтвердил правило: опечатки вроде `выполненых` считать реальными замечаниями и отмечать отдельно.
- 2026-04-30. Пользователь явно подтвердил правило работы с памятью агента: локальная память в `/workspace/memory` является первичным источником истины. GitHub-зеркало `betonglg-ux/Agentglg` должно только дублировать актуальные локальные наработки и не должно автоматически обновлять или заменять память агента в обратную сторону. При расхождениях сначала сравнивать локальную память, защитные снимки и подтвержденные пользователем правки, а не считать зеркало более актуальным по умолчанию.
- 2026-04-30. Пользователь повторно и явно подтвердил расширенное правило работы с памятью и инструкциями агента: локальная память в `/workspace/memory` всегда первична для текущей работы; нельзя автоматически подменять ее данными из GitHub-зеркала; синхронизация разрешена только в сторону `текущий агент -> GitHub-зеркало`; при расхождениях нельзя автоматически перезаписывать локальную память или инструкции данными из зеркала; сначала нужно сравнить локальную память, защитные снимки и подтвержденные пользователем правки. Англоязычный блок про memory folder тоже должен сохранять тот же смысл: local memory is primary working memory, and GitHub mirror must not override newer local memory by default.
- 2026-05-03. Пользователь явно подтвердил дополнительное правило синхронизации: перед обновлением GitHub-зеркала нужно сначала проверить текущую рабочую среду агента и выгружать только реальные изменения. Если локальные служебные копии в `agent_files` расходятся с актуальными инструкциями и памятью, нельзя автоматически считать их более правильными и нельзя откатывать зеркало их устаревшим содержимым. При расхождениях сначала сравнивать локальную память, защитные снимки и подтвержденные пользователем правки; синхронизация разрешена только в сторону `текущий агент -> GitHub-зеркало`.
- 2026-05-04. Пользователь явно подтвердил правило для УИ-протоколов по шаблону `2.0 ИПС универсальная`: запись в выводе вида `27,3 - 30,6 МПа` и `71,1 - 79,8%`, где единица измерения указана только после второго значения диапазона, является допустимой. Это не считать ошибкой и не выносить в замечания.
- 2026-05-04. Пользователь явно снял ранее вынесенные замечания и подтвердил, что для их протоколов не считать ошибкой служебную нумерацию страниц в нижнем колонтитуле.
- 2026-05-04. Пользователь явно снял ранее вынесенные замечания и подтвердил, что для их протоколов формулировки в выводах `менее 28 суток` и `более 28 суток` не считать ошибкой и не выносить в замечания.
- 2026-05-04. Пользователь явно снял ранее вынесенные замечания и подтвердил, что артефакты итоговой сборки PDF, где на лист может заезжать линия или фрагмент соседней страницы, не считать дефектом протокола и не выносить в замечания.
- 2026-05-04. Пользователь явно подтвердил дополнительное правило проверки: по умолчанию отдельно проверять орфографию, грамматику и текстовые блоки в шапках, заголовках таблиц, выводах и наименованиях конструкций.
- 2026-05-04. Пользователь явно подтвердил дополнительное правило для УК-протоколов с `Приложением А`: все даты на схеме нужно отдельно сверять с датами бетонирования в основном протоколе. Если в легенде схемы есть дата, которой нет в таблице основного протокола, это является важным замечанием. Подтвержденный пример: стр. `15`, протокол `295.3.УК`, дата `17.04.2026`.
- 2026-05-04. Пользователь явно подтвердил дополнительное визуальное правило: локальные разрывы рамок и границ таблиц нужно фиксировать даже тогда, когда остальная таблица выглядит нормально. Подтвержденный пример: стр. `2`, протокол `295.1.УК`, правая часть нижней границы под блоком значений рядом с датой `15.04.2026` и строкой с числом `3466`.
- 2026-05-04. Пользователь явно подтвердил новое обязательное правило работы с памятью и ответами в Slack: агент не имеет права писать пользователю, что `запомнил`, `сохранил`, `добавил в память` или `учел для будущих проверок`, пока правило не записано в постоянные файлы памяти реально. Правильная последовательность действий: сначала коротко сообщить пользователю, что агент сейчас сохраняет правило, затем выполнить реальную запись в память, затем отдельным сообщением явно подтвердить итог: `сохранил` или `не удалось сохранить`.
